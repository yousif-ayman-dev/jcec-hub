(() => {
  const i18n = {
    ar: {
      nav_home: "الرئيسية",
      nav_about: "من نحن",
      nav_services: "الخدمات",
      nav_gallery: "المعرض",
      nav_hours: "ساعات العمل",
      nav_contact: "تواصل معنا",
      wa_cta: "احجز عبر واتساب",
      hero_title: "مساحتك الهادئة للعمل والإبداع في غزة",
      hero_sub: "مكان مجهّز للعمل، الاجتماعات، وورش التدريب بأسلوب احترافي ومريح.",
      hero_btn: "احجز عبر واتساب",
      highlights_title: "مساحاتنا وخدماتنا",
      services_title: "الخدمات",
      service_1: "مساحة عمل مشتركة",
      service_2: "مكاتب خاصة",
      service_3: "قاعات اجتماعات",
      service_4: "قاعات تدريب",
      service_desc: "بيئة منظمة وهادئة مناسبة للإنتاجية.",
      services_note: "بيئة منظمة وهادئة مناسبة للإنتاجية.",
      cta_title: "جاهز لتبدأ يومك بإنتاجية أعلى؟",
      cta_sub: "تواصل معنا الآن واحجز مساحتك المناسبة بسهولة.",
      location_title: "الموقع",
      location_more: "عرض تفاصيل التواصل",
      address: "السرايا – بعد منتزه البلدية بـ50 متر – عمارة دياموند – الطابق الأول",
      footer_address_short: "السرايا – عمارة دياموند – الطابق الأول",
      logo_alt: "شعار jcec.hub",
      about_title: "من نحن",
      about_text: "jcec.hub مساحة عمل حديثة في غزة تم تصميمها لدعم رواد الأعمال، الفرق الصغيرة، والمدربين ضمن بيئة احترافية مريحة.",
      affiliation: "jcec.hub نشاط مستقل ويتبع لشركة jcec.company (شركة إنشاءات الجزيرة).",
      gallery_title: "المعرض",
      gallery_sub: "لمحات من بيئة العمل داخل jcec.hub",
      hours_title: "ساعات العمل",
      hours_main: "يوميًا من 12:00 ظهرًا إلى 10:00 مساءً",
      hours_note: "قد تتغير المواعيد، تواصل معنا للتأكيد.",
      contact_title: "تواصل معنا",
      contact_address_label: "العنوان:",
      contact_whatsapp_label: "واتساب:",
      contact_email_label: "البريد الإلكتروني:",
      contact_form_title: "أرسل رسالة",
      form_name: "الاسم",
      form_email: "البريد الإلكتروني",
      form_message: "الرسالة",
      form_submit: "إرسال عبر واتساب",
      contact_whatsapp: "واتساب",
      footer_tag: "مساحة عمل احترافية في غزة.",
      footer_links: "روابط سريعة",
      footer_contact: "التواصل",
      footer_social: "تابعنا",
      footer_rights: "جميع الحقوق محفوظة.",
      skip_to_content: "تخطي إلى المحتوى",
      nav_toggle_aria: "فتح أو إغلاق القائمة",
      lang_toggle_aria: "تبديل اللغة",
      floating_wa_label: "تواصل واتساب",
      wa_tooltip: "احجز عبر واتساب",
      instagram_aria: "حساب انستغرام jcec.hub",
      facebook_aria: "صفحة فيسبوك jcec.hub",
      whatsapp_prefill: "مرحبًا jcec.hub، أرغب بالتواصل.",
      dev_credit_link: "تنفيذ الموقع: Yousif Ayman (اضغط لزيارة موقعي)",
      dev_credit_note: "YAD Services | تطوير مواقع",
      dev_whatsapp_cta: "تواصل مع فريق التطوير عبر واتساب",
      dev_spotlight_title: "هل تحتاج موقعًا بنفس الجودة؟",
      dev_spotlight_text: "إذا أعجبك هذا الموقع، يمكن تنفيذ موقع مشابه لهوية مشروعك بشكل احترافي وسريع.",
      dev_spotlight_cta: "اطلب موقعك الآن",
      dev_spotlight_note: "ملاحظة: رابط البورتفوليو الخاص بالمطور موجود أسفل الصفحة في الفوتر.",
      faq_title: "الأسئلة الشائعة",
      faq_q1: "كيف أحجز مساحة أو مكتب؟",
      faq_a1: "يمكنك الحجز مباشرة عبر واتساب، وسننسق معك الموعد ونوع المساحة المناسبة.",
      faq_q2: "هل يوجد إنترنت وكهرباء بشكل مستقر؟",
      faq_a2: "نعم، المكان مجهز بخدمات تشغيل أساسية مناسبة للعمل اليومي والاجتماعات.",
      faq_q3: "هل يمكن الحجز لساعات أو ليوم كامل؟",
      faq_a3: "نعم، تتوفر خيارات مرنة حسب مدة الاستخدام ونوع المساحة المطلوبة."
    },
    en: {
      nav_home: "Home",
      nav_about: "About",
      nav_services: "Services",
      nav_gallery: "Gallery",
      nav_hours: "Working Hours",
      nav_contact: "Contact",
      wa_cta: "Book via WhatsApp",
      hero_title: "Your professional workspace in Gaza",
      hero_sub: "A comfortable space for work, meetings, and training sessions.",
      hero_btn: "Book via WhatsApp",
      highlights_title: "Our Spaces & Services",
      services_title: "Services",
      service_1: "Shared Workspace",
      service_2: "Private Offices",
      service_3: "Meeting Rooms",
      service_4: "Training Rooms",
      service_desc: "A clean and organized environment designed for productivity.",
      services_note: "A clean and organized environment designed for productivity.",
      cta_title: "Ready to start a more productive day?",
      cta_sub: "Contact us now and book the workspace that fits your needs.",
      location_title: "Location",
      location_more: "View contact details",
      address: "Al Saraya – 50m after Al Baladiya Park – Diamond Building – 1st Floor",
      footer_address_short: "Al Saraya - Diamond Building - 1st Floor",
      logo_alt: "jcec.hub logo",
      about_title: "About Us",
      about_text: "jcec.hub is a modern workspace in Gaza built to support entrepreneurs, small teams, and trainers in a professional, comfortable setting.",
      affiliation: "jcec.hub operates independently and is affiliated with jcec.company (Al Jazeera Construction).",
      gallery_title: "Gallery",
      gallery_sub: "Snapshots from the working environment at jcec.hub",
      hours_title: "Working Hours",
      hours_main: "Daily from 12:00 PM to 10:00 PM",
      hours_note: "Hours may change, contact us to confirm.",
      contact_title: "Contact Us",
      contact_address_label: "Address:",
      contact_whatsapp_label: "WhatsApp:",
      contact_email_label: "Email:",
      contact_form_title: "Send a Message",
      form_name: "Name",
      form_email: "Email",
      form_message: "Message",
      form_submit: "Send via WhatsApp",
      contact_whatsapp: "WhatsApp",
      footer_tag: "A professional workspace in Gaza.",
      footer_links: "Quick Links",
      footer_contact: "Contact",
      footer_social: "Follow Us",
      footer_rights: "All rights reserved.",
      skip_to_content: "Skip to content",
      nav_toggle_aria: "Open or close menu",
      lang_toggle_aria: "Switch language",
      floating_wa_label: "Open WhatsApp chat",
      wa_tooltip: "Book via WhatsApp",
      instagram_aria: "jcec.hub Instagram profile",
      facebook_aria: "jcec.hub Facebook page",
      whatsapp_prefill: "Hello jcec.hub, I would like to get in touch.",
      dev_credit_link: "Website by Yousif Ayman (Tap to visit my site)",
      dev_credit_note: "YAD Services | Web Development",
      dev_whatsapp_cta: "Chat with the development team on WhatsApp",
      dev_spotlight_title: "Need a Similar Website?",
      dev_spotlight_text: "If you like this website, a similar one can be built for your project with a professional and fast delivery.",
      dev_spotlight_cta: "Request Your Website",
      dev_spotlight_note: "Note: The developer portfolio link is available in the footer below.",
      faq_title: "Frequently Asked Questions",
      faq_q1: "How can I book a workspace or office?",
      faq_a1: "Book directly via WhatsApp and we will confirm your timing and the suitable space.",
      faq_q2: "Do you provide stable internet and power?",
      faq_a2: "Yes, the hub is equipped with core utilities suitable for daily work and meetings.",
      faq_q3: "Can I book by hour or for a full day?",
      faq_a3: "Yes, flexible booking options are available based on your duration and space type."
    }
  };

  const html = document.documentElement;
  const body = document.body;
  const langSwitch = document.getElementById("langSwitch");
  const headerActions = document.querySelector(".header-actions");
  const navMenu = document.getElementById("navMenu");
  const menuToggle = document.querySelector(".menu-toggle");
  const normalizeLang = (value) => (value === "en" ? "en" : "ar");
  let currentLang = "ar";
  const getLang = () => currentLang;
  const persistLang = (lang) => {
    const nextLang = normalizeLang(lang);
    currentLang = nextLang;
    return nextLang;
  };
  const syncLangInInternalLinks = (lang) => {
    document.querySelectorAll('a[href]').forEach((link) => {
      const href = link.getAttribute("href");
      if (!href || href.startsWith("#") || href.startsWith("mailto:") || href.startsWith("tel:") || href.startsWith("javascript:")) return;
      let parsed;
      try {
        parsed = new URL(href, window.location.href);
      } catch (_) {
        return;
      }
      const isHttp = parsed.protocol === "http:" || parsed.protocol === "https:";
      const isFile = parsed.protocol === "file:";
      if (!isHttp && !isFile) return;
      const path = parsed.pathname.toLowerCase();
      const isSitePage = /\/(index|about|services|gallery|hours|contact)\.html$/.test(path);
      if (!isSitePage) return;
      parsed.searchParams.delete("lang");
      if (lang === "en") parsed.searchParams.set("lang", "en");
      const nextHref = (isFile ? parsed.pathname : `${parsed.pathname}`) + parsed.search + parsed.hash;
      link.setAttribute("href", nextHref);
    });
  };
  if (headerActions && !document.querySelector(".top-social")) {
    const topSocial = document.createElement("div");
    topSocial.className = "top-social";
    topSocial.innerHTML = `
      <a class="social-link" href="https://www.instagram.com/jcec.hub?igsh=OXRsNnc1YWM3eGNt" target="_blank" rel="noopener noreferrer" aria-label="Instagram" data-i18n-aria-label="instagram_aria">
        <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <rect x="3" y="3" width="18" height="18" rx="5" stroke="currentColor" stroke-width="2"></rect>
          <circle cx="12" cy="12" r="4" stroke="currentColor" stroke-width="2"></circle>
          <circle cx="17.5" cy="6.5" r="1.2" fill="currentColor"></circle>
        </svg>
      </a>
      <a class="social-link" href="https://www.facebook.com/share/1FrukVHEnd/" target="_blank" rel="noopener noreferrer" aria-label="Facebook" data-i18n-aria-label="facebook_aria">
        <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <path d="M14.5 8H16V5h-2c-2.2 0-3.5 1.4-3.5 3.7V11H8v3h2.5v5h3v-5H16l.5-3h-3V9c0-.7.3-1 1-1Z" fill="currentColor"></path>
        </svg>
      </a>
    `;
    headerActions.prepend(topSocial);
  }

  const ensureDeveloperSpotlight = () => {
    const currentPage = html.dataset.page;
    if (currentPage !== "home" && currentPage !== "contact") return;

    const footer = document.querySelector(".site-footer");
    if (!footer || document.querySelector(".dev-showcase")) return;

    const section = document.createElement("section");
    section.className = "section dev-showcase";
    section.innerHTML = `
      <div class="container dev-showcase-wrap reveal">
        <h2 data-i18n="dev_spotlight_title">هل تحتاج موقعًا بنفس الجودة؟</h2>
        <p data-i18n="dev_spotlight_text">إذا أعجبك هذا الموقع، يمكن تنفيذ موقع مشابه لهوية مشروعك بشكل احترافي وسريع.</p>
        <a class="btn btn-accent dev-showcase-btn" href="https://wa.me/972569040508" target="_blank" rel="noopener" data-i18n="dev_spotlight_cta">اطلب موقعك الآن</a>
        <small class="dev-showcase-note" data-i18n="dev_spotlight_note">ملاحظة: رابط البورتفوليو الخاص بالمطور موجود أسفل الصفحة في الفوتر.</small>
      </div>
    `;

    footer.parentNode.insertBefore(section, footer);
  };

  const setTextNodes = (dict) => {
    document.querySelectorAll("[data-i18n]").forEach((el) => {
      const key = el.getAttribute("data-i18n");
      if (dict[key]) el.textContent = dict[key];
    });

    document.querySelectorAll("[data-i18n-aria-label]").forEach((el) => {
      const key = el.getAttribute("data-i18n-aria-label");
      if (dict[key]) el.setAttribute("aria-label", dict[key]);
    });

    document.querySelectorAll("[data-i18n-tooltip]").forEach((el) => {
      const key = el.getAttribute("data-i18n-tooltip");
      if (dict[key]) el.setAttribute("data-tooltip", dict[key]);
    });

    document.querySelectorAll("[data-i18n-alt]").forEach((el) => {
      const key = el.getAttribute("data-i18n-alt");
      if (dict[key]) el.setAttribute("alt", dict[key]);
    });
  };

  const setDeveloperCredit = (dict) => {
    const creditLink = document.querySelector(".dev-credit a");
    const creditNote = document.querySelector(".dev-credit span");
    const creditWrap = document.querySelector(".dev-credit");
    const devWaHref = "https://wa.me/972569040508";
    if (creditLink && dict.dev_credit_link) creditLink.textContent = dict.dev_credit_link;
    if (creditNote && dict.dev_credit_note) creditNote.textContent = dict.dev_credit_note;
    if (creditWrap) {
      let devWaLink = creditWrap.querySelector(".dev-wa-link");
      if (!devWaLink) {
        devWaLink = document.createElement("a");
        devWaLink.className = "dev-wa-link";
        devWaLink.href = devWaHref;
        devWaLink.target = "_blank";
        devWaLink.rel = "noopener noreferrer";
        creditWrap.appendChild(devWaLink);
      }
      if (dict.dev_whatsapp_cta) devWaLink.textContent = dict.dev_whatsapp_cta;
    }
  };

  const setPageTitle = (lang) => {
    const page = html.dataset.page;
    const pageTitleMap = {
      home: lang === "ar" ? "jcec.hub | الرئيسية" : "jcec.hub | Home",
      about: lang === "ar" ? "jcec.hub | من نحن" : "jcec.hub | About",
      services: lang === "ar" ? "jcec.hub | الخدمات" : "jcec.hub | Services",
      gallery: lang === "ar" ? "jcec.hub | المعرض" : "jcec.hub | Gallery",
      hours: lang === "ar" ? "jcec.hub | ساعات العمل" : "jcec.hub | Working Hours",
      contact: lang === "ar" ? "jcec.hub | تواصل معنا" : "jcec.hub | Contact"
    };

    if (pageTitleMap[page]) document.title = pageTitleMap[page];
  };

  const applyLanguage = (lang) => {
    const dict = i18n[lang];
    html.lang = lang;
    html.dir = lang === "ar" ? "rtl" : "ltr";
    body.classList.toggle("lang-en", lang === "en");

    setTextNodes(dict);
    setDeveloperCredit(dict);

    if (langSwitch) {
      langSwitch.textContent = lang === "ar" ? "EN" : "AR";
      langSwitch.setAttribute("aria-label", dict.lang_toggle_aria);
    }

    setPageTitle(lang);
  };

  persistLang("ar");
  ensureDeveloperSpotlight();
  syncLangInInternalLinks(getLang());
  applyLanguage(getLang());

  if (langSwitch) {
    langSwitch.addEventListener("click", () => {
      langSwitch.classList.add("switching");
      const next = persistLang(getLang() === "ar" ? "en" : "ar");
      syncLangInInternalLinks(next);
      applyLanguage(next);
      setTimeout(() => langSwitch.classList.remove("switching"), 320);
    });
  }

  let navBackdrop = document.querySelector(".nav-backdrop");
  if (!navBackdrop && navMenu) {
    navBackdrop = document.createElement("div");
    navBackdrop.className = "nav-backdrop";
    document.body.appendChild(navBackdrop);
  }

  const closeMobileNav = () => {
    if (!navMenu || !menuToggle) return;
    navMenu.classList.remove("open");
    menuToggle.classList.remove("open");
    menuToggle.setAttribute("aria-expanded", "false");
    if (navBackdrop) navBackdrop.classList.remove("show");
  };

  if (menuToggle && navMenu) {
    menuToggle.addEventListener("click", () => {
      const expanded = menuToggle.getAttribute("aria-expanded") === "true";
      menuToggle.setAttribute("aria-expanded", String(!expanded));
      navMenu.classList.toggle("open", !expanded);
      menuToggle.classList.toggle("open", !expanded);
      if (navBackdrop) navBackdrop.classList.toggle("show", !expanded);
    });

    navMenu.querySelectorAll("a").forEach((a) => {
      a.addEventListener("click", closeMobileNav);
    });

    if (navBackdrop) {
      navBackdrop.addEventListener("click", closeMobileNav);
    }

    window.addEventListener("resize", () => {
      if (window.innerWidth > 860) closeMobileNav();
    });
  }

  const page = html.dataset.page;
  document.querySelectorAll(".main-nav [data-nav]").forEach((a) => {
    if (a.dataset.nav === page) a.classList.add("active");
  });

  const hero = document.querySelector(".hero");
  if (page === "home" && hero) {
    const heroSequence = [
      { src: "assets/images/img7.jpg", duration: 10000 },
      { src: "assets/images/img5.jpg", duration: 5000 },
      { src: "assets/images/img8.jpg", duration: 5000 }
    ];

    let currentImageIndex = 0;
    const applyHeroImage = () => {
      const current = heroSequence[currentImageIndex];
      hero.style.backgroundImage = `url("${current.src}")`;
      setTimeout(() => {
        currentImageIndex = (currentImageIndex + 1) % heroSequence.length;
        applyHeroImage();
      }, current.duration);
    };

    applyHeroImage();
  }

  document.querySelectorAll(".card-grid, .gallery-grid").forEach((group) => {
    const items = group.querySelectorAll(".reveal");
    items.forEach((item, index) => {
      item.style.setProperty("--delay", `${index * 70}ms`);
    });
  });

  document.querySelectorAll(".section-title.reveal").forEach((item) => {
    item.style.setProperty("--delay", "40ms");
  });

  const revealItems = document.querySelectorAll(".reveal");
  const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  if (!prefersReducedMotion && "IntersectionObserver" in window) {
    const revealObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("visible");
            revealObserver.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.12 }
    );

    revealItems.forEach((el) => revealObserver.observe(el));
  } else {
    revealItems.forEach((el) => el.classList.add("visible"));
  }

  const year = document.getElementById("year");
  if (year) year.textContent = new Date().getFullYear();

  const galleryItems = document.querySelectorAll("[data-lightbox-src]");
  const lightbox = document.getElementById("lightbox");
  const lightboxImg = document.getElementById("lightboxImg");
  const lightboxClose = document.getElementById("lightboxClose");

  if (galleryItems.length && lightbox && lightboxImg && lightboxClose) {
    galleryItems.forEach((item) => {
      item.addEventListener("click", () => {
        lightboxImg.src = item.dataset.lightboxSrc;
        lightbox.classList.add("show");
        lightbox.setAttribute("aria-hidden", "false");
        body.style.overflow = "hidden";
      });
    });

    const closeLightbox = () => {
      lightbox.classList.remove("show");
      lightbox.setAttribute("aria-hidden", "true");
      lightboxImg.src = "";
      body.style.overflow = "";
    };

    lightboxClose.addEventListener("click", closeLightbox);
    lightbox.addEventListener("click", (e) => {
      if (e.target === lightbox) closeLightbox();
    });

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && lightbox.classList.contains("show")) closeLightbox();
    });
  }

  const contactForm = document.getElementById("contactForm");
  const trackWhatsAppClick = (origin) => {
    if (typeof window.gtag === "function") {
      window.gtag("event", "whatsapp_click", {
        event_category: "engagement",
        event_label: origin,
        page_path: window.location.pathname
      });
    }

    if (Array.isArray(window.dataLayer)) {
      window.dataLayer.push({
        event: "whatsapp_click",
        origin,
        page_path: window.location.pathname
      });
    }

    const countKey = "jcec_whatsapp_click_count";
    const previousCount = Number(localStorage.getItem(countKey) || "0");
    localStorage.setItem(countKey, String(previousCount + 1));
  };

  document.querySelectorAll('a[href*="wa.me/"]').forEach((link) => {
    link.addEventListener("click", () => {
      trackWhatsAppClick(link.className || "wa_link");
    });
  });

  if (contactForm) {
    contactForm.addEventListener("submit", (e) => {
      e.preventDefault();
      const lang = getLang();
      const name = document.getElementById("name").value.trim();
      const email = document.getElementById("email").value.trim();
      const message = document.getElementById("message").value.trim();
      const greeting = i18n[lang].whatsapp_prefill;
      const composed = `${greeting}\nName: ${name}\nEmail: ${email}\nMessage: ${message}`;
      const url = `https://wa.me/970567100081?text=${encodeURIComponent(composed)}`;
      trackWhatsAppClick("contact_form");
      window.open(url, "_blank", "noopener");
    });
  }

  const waFab = document.querySelector(".floating-wa");
  if (waFab) {
    const posKey = "jcec_wa_fab_position_v1";
    let isPointerDown = false;
    let isDragging = false;
    let startX = 0;
    let startY = 0;
    let originX = 0;
    let originY = 0;
    let suppressClick = false;

    const clamp = (value, min, max) => Math.min(Math.max(value, min), max);

    const applyPosition = (x, y) => {
      const pad = 8;
      const maxX = Math.max(pad, window.innerWidth - waFab.offsetWidth - pad);
      const maxY = Math.max(pad, window.innerHeight - waFab.offsetHeight - pad);
      const nextX = clamp(x, pad, maxX);
      const nextY = clamp(y, pad, maxY);
      waFab.style.left = `${nextX}px`;
      waFab.style.top = `${nextY}px`;
      waFab.style.right = "auto";
      waFab.style.bottom = "auto";
      return { x: nextX, y: nextY };
    };

    const savePosition = (x, y) => {
      localStorage.setItem(posKey, JSON.stringify({ x, y }));
    };

    const restorePosition = () => {
      const raw = localStorage.getItem(posKey);
      if (!raw) return;
      try {
        const parsed = JSON.parse(raw);
        if (typeof parsed.x === "number" && typeof parsed.y === "number") {
          applyPosition(parsed.x, parsed.y);
        }
      } catch (_) {
        localStorage.removeItem(posKey);
      }
    };

    restorePosition();

    waFab.addEventListener("pointerdown", (e) => {
      if (e.button !== 0 && e.pointerType !== "touch") return;
      isPointerDown = true;
      isDragging = false;
      startX = e.clientX;
      startY = e.clientY;
      const rect = waFab.getBoundingClientRect();
      originX = rect.left;
      originY = rect.top;
      waFab.setPointerCapture(e.pointerId);
    });

    waFab.addEventListener("pointermove", (e) => {
      if (!isPointerDown) return;
      const dx = e.clientX - startX;
      const dy = e.clientY - startY;
      if (!isDragging && Math.hypot(dx, dy) > 4) {
        isDragging = true;
        waFab.classList.add("dragging");
      }
      if (!isDragging) return;
      e.preventDefault();
      applyPosition(originX + dx, originY + dy);
    });

    const stopDrag = (e) => {
      if (!isPointerDown) return;
      isPointerDown = false;
      waFab.classList.remove("dragging");
      waFab.releasePointerCapture?.(e.pointerId);
      if (isDragging) {
        const rect = waFab.getBoundingClientRect();
        savePosition(rect.left, rect.top);
        suppressClick = true;
        setTimeout(() => {
          suppressClick = false;
        }, 120);
      }
      isDragging = false;
    };

    waFab.addEventListener("pointerup", stopDrag);
    waFab.addEventListener("pointercancel", stopDrag);
    waFab.addEventListener("click", (e) => {
      if (suppressClick) {
        e.preventDefault();
        e.stopPropagation();
      }
    });

    window.addEventListener("resize", () => {
      const raw = localStorage.getItem(posKey);
      if (!raw) return;
      try {
        const parsed = JSON.parse(raw);
        if (typeof parsed.x === "number" && typeof parsed.y === "number") {
          const fixed = applyPosition(parsed.x, parsed.y);
          savePosition(fixed.x, fixed.y);
        }
      } catch (_) {
        localStorage.removeItem(posKey);
      }
    });
  }
})();
